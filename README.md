# Task Orbit

A clean, responsive todo app built with plain HTML, CSS, and JavaScript.

## Features
- Add, complete, and delete tasks
- Clear all completed tasks
- Task counts (total and pending)
- Data persistence with `localStorage`
- Mobile-friendly responsive UI

## Local run
Open `index.html` directly in your browser, or run a local static server:

```bash
python3 -m http.server 5500
```

Then open `http://localhost:5500`.

## Push to GitHub
```bash
git init
git add .
git commit -m "Build polished todo app UI and logic"
git branch -M main
git remote add origin <your-repo-url>
git push -u origin main
```


# Todo-app✨
